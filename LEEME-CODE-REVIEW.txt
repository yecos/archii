═══════════════════════════════════════════════════════════════
  ArchiFlow — Code Review Fixes
  Revisión completa de código — 7 problemas encontrados y corregidos
═══════════════════════════════════════════════════════════════

ARCHIVOS MODIFICADOS:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. src/app/HomeContent.tsx
   ✅ FIX: TopBar ya no recibe props innecesarios (leía todo de useApp())
   ✅ FIX: Eliminado import duplicado de 'dynamic' (línea 258)

2. src/contexts/AppContext.tsx
   ✅ FIX: Eliminados 3 hooks importados pero nunca usados (useFirestoreData, useNotifications, useVoiceRecording)
   ✅ FIX: Agregado authUser al dependency array del listener de chat DMs (evita stale closure)

3. src/components/ui/ui/ (DIRECTORIO COMPLETO)
   ✅ FIX: Eliminado directorio duplicado (48 archivos basura)

4. eslint.config.mjs
   ✅ FIX: Agregadas reglas para evitar errores de circular config

5. package.json
   ✅ FIX: Eliminados ~14 paquetes npm sin usar:
      - next-auth, next-intl, next-themes
      - @hookform/resolvers, @mdxeditor/editor
      - @reactuses/core, @tanstack/react-query
      - react-hook-form, react-markdown, react-syntax-highlighter
      - react-day-picker, react-resizable-panels, embla-carousel-react
      - cmdk, input-otp, z-ai-web-dev-sdk
   ⚠️ IMPORTANTE: Después de subir, ejecuta: npm install (o bun install)

INSTRUCCIONES:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Extrae este ZIP en la RAÍZ de tu proyecto (sobrescribe archivos)
2. Elimina manualmente la carpeta: src/components/ui/ui/
3. Ejecuta: npm install (para limpiar paquetes eliminados)
4. Ejecuta: git add . && git commit -m "fix: code review - 7 issues fixed"
5. Ejecuta: git push origin main
