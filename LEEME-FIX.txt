ArchiFlow — Corrección Date Picker + OneDrive
===============================================

ARCHIVOS MODIFICADOS:
  src/app/globals.css
  src/contexts/AppContext.tsx

INSTRUCCIONES:
  1. Reemplaza estos 2 archivos en tu repositorio local
  2. Haz git add, commit y push
  3. Vercel desplegará automáticamente

FIX 1 — Date Picker (calendario no aparece en modales):
  - globals.css: Se agregó touch-action: manipulation en inputs, select y textarea
    dentro de [data-vaul-drawer] para permitir el popup del calendario nativo

FIX 2 — OneDrive (Error interno del proveedor Microsoft):
  - AppContext.tsx: Se revirtió prompt de 'consent' a 'select_account' y se agregó
    fallback automático (login básico sin scopes si falla con scopes de OneDrive)
